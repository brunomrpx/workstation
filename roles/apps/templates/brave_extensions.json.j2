{
  "ExtensionSettings": {
{% for ext_id in brave_extension_ids %}
    "{{ ext_id }}": {
      "installation_mode": "force_installed",
      "update_url": "https://clients2.google.com/service/update2/crx"
    }{% if not loop.last %},{% endif %}

{% endfor %}
  }
}
