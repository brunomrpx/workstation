[defaults]
inventory = inventory.yml
roles_path = roles
result_format = yaml
host_key_checking = False
retry_files_enabled = False

[privilege_escalation]
# Ubuntu 26.04 defaults sudo to sudo-rs, which breaks Ansible's become password
# prompt flow. sudo.ws is the classic GNU sudo binary Ubuntu ships alongside it.
become_exe = sudo.ws
